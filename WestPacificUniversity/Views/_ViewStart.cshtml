@*
  https://learn.microsoft.com/en-us/aspnet/core/mvc/views/layout?view=aspnetcore-7.0

  Every veiw/page starts with this file.
*@

@{
    // Layout defines top level template for views/pages in the app.

    // By convention, the default layout file is Views/Shared/_Layout.cshtml
    // Individual views can override the Layout property to specify a view-specific layout.
    // Asp.Net Core follows the same discovery process as partial view to discovery layout
    // file if a full path is not specified.
    // https://learn.microsoft.com/en-us/aspnet/core/mvc/views/partial?view=aspnetcore-7.0#partial-view-discovery
    Layout = "_Layout";
}
